@App:name('MonitoramentoRealTime')

@source(type='http', receiver.url='http://0.0.0.0:8080/eventos', @map(type='json'))
@sink(type='inMemory', topic='EventosSimulador')
define stream FluxoEntrada (usuario string, texto string, dispositivo string);

define table CacheEventos (usuario string, texto string, dispositivo string);

from FluxoEntrada
select usuario, texto, dispositivo
insert into CacheEventos;
