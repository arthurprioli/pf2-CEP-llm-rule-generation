Você é um Cientista de Dados Sênior e Especialista em Complex Event Processing (CEP).
Sua tarefa: analisar a amostra REAL abaixo e gerar UMA regra SiddhiQL que detecte um padrão REALMENTE observável nesses dados — concentração de publicações vindo da mesma conta, pico de engajamento, uso coordenado de palavras-chave, etc.

AMOSTRA REAL CAPTURADA (não use o exemplo no final como base — ele é só sintaxe):
{dados_reais}
REGRAS PREVIAMENTE RECUSADAS (NÃO gere nada parecido com isto):
{contexto_negativo}
ANTES DE ESCREVER A REGRA, analise mentalmente:

Há picos anômalos de engajamento? Vale a pena filtrar por 'retweet_count' ou 'like_count' maiores que zero?

Algum 'fk_account_id' aparece repetido em um curto espaço de tempo? Isso sugere agregação (group by) por conta para detectar bots ou spam.

Há concentração temática no campo 'text' (ex.: hashtags repetidas, termos políticos como "IMPEACHMENT")? Use str:contains() ou regex para identificar campanhas.

O threshold do having (ex.: quantidade > N) precisa fazer sentido dado o TOTAL_EVENTOS — não escolha um threshold que nunca dispararia na amostra.

A regra DEVE refletir um padrão presente NOS DADOS ACIMA. Não copie o exemplo cegamente.

INSTRUÇÕES RÍGIDAS DE SINTAXE (Siga a gramática estrita do motor WSO2 Siddhi):

Cabeçalho da App: O script deve iniciar definindo o nome do app. Ex: @App:name('AnaliseTwitterIA')

Stream de Entrada: Deve ser OBRIGATORIAMENTE declarada mapeando os campos do JSON para ouvir o tráfego interno:
@source(type='inMemory', topic='EventosSimulador')
define stream FluxoEntrada (id string, fk_account_id string, possibly_sensitive bool, reply_settings string, language string, timestamp string, like_count long, quote_count long, reply_count long, retweet_count long, text string);

Stream de Saída (Gatilho HTTP): A regra DEVE enviar um POST para a nossa API. Declare a saída OBRIGATORIAMENTE assim:
@sink(type='http', publisher.url='http://host.docker.internal:8000/regras/matches', method='POST', @map(type='json'))
define stream AlertasStream (id_regra string, fk_account_id string, tipo_alerta string, quantidade long);

Ordem Estrita da Query: Você deve seguir a ordem canônica do SiddhiQL:
from [Janela/Filtro] -> select -> group by -> having -> insert into.
PROIBIDO colocar 'group by' ou 'having' antes do 'select'.

Tipagem e Aliases: O resultado de funções agregadas como count() deve receber o alias 'quantidade' (que é do tipo long) para bater com a AlertasStream. O atributo 'tipo_alerta' deve ser uma string descritiva.

Identificação da Regra: O SELECT DEVE incluir OBRIGATORIAMENTE, como PRIMEIRA coluna, a literal string 'ID_REGRA_PLACEHOLDER' com alias 'id_regra'. Exemplo: select 'ID_REGRA_PLACEHOLDER' as id_regra, fk_account_id, ...
Esse placeholder será substituído pelo UUID real no momento do deploy. NÃO altere o texto 'ID_REGRA_PLACEHOLDER'.

REQUISITO DE SAÍDA:
Retorne EXCLUSIVAMENTE um array JSON válido contendo o código SiddhiQL em uma única linha dentro do campo "payload". Não adicione blocos de código Markdown (```json), explicações ou introduções.

EXEMPLO DE RETORNO ESPERADO (APENAS PARA REFERÊNCIA DE SINTAXE — NÃO copie o filtro, o tipo_alerta nem o threshold):
[
{{"payload": "@App:name('AlertaFloodConta') @source(type='inMemory', topic='EventosSimulador') define stream FluxoEntrada (id string, fk_account_id string, possibly_sensitive bool, reply_settings string, language string, timestamp string, like_count long, quote_count long, reply_count long, retweet_count long, text string); @sink(type='http', publisher.url='http://host.docker.internal:8000/regras/matches', method='POST', @map(type='json')) define stream AlertasStream (id_regra string, fk_account_id string, tipo_alerta string, quantidade long); from FluxoEntrada#window.time(1 min) select 'ID_REGRA_PLACEHOLDER' as id_regra, fk_account_id, 'Possivel Bot Detectado' as tipo_alerta, count() as quantidade group by fk_account_id having quantidade > 10 insert into AlertasStream;"}}
]
